# Flutter / Dart build outputs
build/
.dart_tool/
.packages
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/

# Windows runner generated by CI (flutter create in workflow) or locally.
# NEVER commit this — it is regenerated fresh on every cloud build.
windows/

# IDE / editor noise
.idea/
.vscode/
*.iml
*.swp

# OS noise
Thumbs.db
.DS_Store

# Misc
*.log
